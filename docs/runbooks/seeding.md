# Seeding

Run:

```bash
pnpm db:seed
```

Seed includes:

- roles;
- permissions;
- local legal placeholder;
- booking hold TTL setting;
- mock mode feature flag;
- optional super admin from `SUPER_ADMIN_TELEGRAM_ID`.

No hardcoded passwords are used.
